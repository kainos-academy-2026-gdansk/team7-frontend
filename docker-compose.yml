services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: team7-frontend:compose
    ports:
      - "4003:4000"
    environment:
      SESSION_SECRET: ${SESSION_SECRET:?SESSION_SECRET must be set}
      NODE_ENV: development
