name: Practice Job

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  practice:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Team 7 practice job works"

      - name: Show GitHub information
        run: |
          echo "Operating system: $RUNNER_OS"
          echo "Event: ${{ github.event_name }}"
          echo "Branch: ${{ github.ref_name }}"
